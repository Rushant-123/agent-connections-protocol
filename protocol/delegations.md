# Delegations

`delegations.md` records active and expired permissions granted to agents.

A delegation should say who can do what, under which constraints, and until when.

## Active Delegations

Required columns:

- `Agent`
- `Scope`
- `Constraints`
- `Expires`
- `Status`
