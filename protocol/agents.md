# Agents

`agents.md` is the objective directory.

It records agent identity, provider metadata, endpoints, supported protocols, claimed capabilities, supported experiences, and verification claims.

## Agent: `<agent-id>`

Required fields:

- `ID`
- `Provider`
- `Endpoint`
- `Protocols`
- `Status`

Recommended sections:

- `Claimed Capabilities`
- `Supported Experiences`
- `Verification`
