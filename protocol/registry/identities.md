# Agent Identities

`.agent-registry/identities.md` records identity claims for agents, providers, and gateways.

Identity can be weak or strong:

- `self_attested`
- `domain_control`
- `did`
- `public_key`
- `oauth_client`
- `organization_verified`

Consumers should treat identity as evidence, not automatic trust.
